plugins {
    id "org.unbroken-dome.test-sets" version "${testSetsPluginVersion}"
}

testSets {
    integrationTest
}

dependencies {
    implementation project(":manual-config")

    integrationTestImplementation project(":in-memory-repository")
    integrationTestImplementation project(":external-find-identity-stub")
    integrationTestImplementation testFixtures(project(":use-cases"))
}