plugins {
    id "org.unbroken-dome.test-sets" version "${testSetsPluginVersion}"
}

testSets {
    integrationTest
}

dependencies {
    implementation project(":identity-config")

    integrationTestImplementation project(":identity-in-memory-repository")
    integrationTestImplementation project(":identity-external-find-stub")
    integrationTestImplementation testFixtures(project(":identity-use-cases"))
    integrationTestImplementation testFixtures(project(":identity-json"))
}