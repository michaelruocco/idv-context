plugins {
    id "java-test-fixtures"
}

dependencies {
    api project(":entities")

    implementation "org.slf4j:slf4j-simple:${slf4jVersion}"

    testFixturesApi testFixtures(project(":entities"))
}